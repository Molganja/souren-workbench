# 素人种草运营工作台 v1

本地网页工作台，用于管理兼职账号、三类内容排期、候选稿、素材扫描、微信交付包、Image 任务预留和抖音核对队列。

## 启动

最简单方式：双击项目根目录的：

```text
启动素人工作台.command
```

它会自动检查环境、安装依赖、构建前端、启动本地服务并打开浏览器。

命令行方式：

```bash
cd /Users/licc/Desktop/素人系统/app
npm install
npm run dev
```

- 前端：`http://127.0.0.1:5173`
- 后端/API：`http://127.0.0.1:5174`

生产构建后也可以只跑后端：

```bash
npm run build
npm run start
```

打开：`http://127.0.0.1:5174`

## 本地数据

系统会自动创建：

```text
/Users/licc/Desktop/素人系统/data/souren.sqlite
/Users/licc/Desktop/素人系统/data/backups/
/Users/licc/Desktop/素人系统/素材库/真实案例/
```

新建案例后，每个案例会自动生成：

```text
00-原始素材/
01-已筛选素材/
02-生成补充/
03-交付给兼职/
04-发布回收/
case.json
```

网页端只保存结构化数据和本地路径，大图片、视频和交付包都放在本地文件夹。

## Image / LLM Key

复制 `.env.example` 为 `.env` 后填写：

```env
IMAGE_API_KEY=
LLM_API_KEY=
```

当前 v1 会创建 Image 任务和 prompt。`IMAGE_API_KEY` 为空时，任务状态为 `waiting_key`，不会影响其他流程。

## 当前闭环

1. 工作人员打开「今日中控台」，只看今天该处理的账号和动作。
2. 新建案例并生成本地案例目录。
   - 新建时会随机生成人设；工作人员只需要补微信昵称、抖音号、主页链接和对接咨询/负责人。
   - 也可以在案例库使用「批量导入」，一次录入多名兼职/账号，并自动生成排期。
3. 把原始素材放进 `00-原始素材/`，点击扫描素材。
4. 系统生成排期槽位，对当天或近期槽位生成 3 条候选稿。
5. 工作人员选择一条并锁定。
6. 生成微信交付包。
   - 如果是视频内容，可以从排期或案例详情创建「剪辑任务」，系统会生成本地剪辑任务单。
7. 工作人员复制文案、打开交付文件夹，用电脑微信发给兼职。
8. 标记派发、兼职已汇报。
9. 打开抖音链接核对并回填状态。
10. 工作人员在「爆款链接」里只粘贴抖音爆款作品链接；青豆提取、拆结构和生成同类型内容由 Codex/AI 处理。

## 验收

```bash
cd /Users/licc/Desktop/素人系统/app
npm run doctor
npm run check
npm run test:e2e
```

`test:e2e` 会临时创建测试数据库和素材目录，跑完后自动清理。

## 批量导入样例

样例文件：

```text
/Users/licc/Desktop/素人系统/app/samples/bulk-import-example.csv
```

可直接复制其中内容到「案例库 → 批量导入」。字段顺序：

```text
微信昵称, 抖音号, 抖音主页链接, 项目, 阶段, 城市, 年龄, 职业, 语气, 动机故事, 对接咨询/负责人
```

## 新增实用操作

- 「排期规划」页可以按未来 7/14/30/60 天、状态、内容类型和账号关键词查看全局排期，并直接处理生成、交付、派发和核对动作。
- 「今日中控台」会按对接咨询/负责人汇总今日任务，工作人员能直接知道每条内容该找谁。
- 案例库支持批量导入兼职/账号，适合一次录入 40+ 账号。
- 案例库支持按关键词、阶段、健康状态搜索筛选，也可以删除误建案例。
- 案例详情里可以直接「打开素材目录」。
- 案例详情里可以编辑案例信息，也可以手动新增临时内容槽位。
- 案例详情会按项目模板显示素材缺口和账号健康提示。
- 异常账号会显示运营建议，把断更、待核对、素材缺口、播放下滑转成下一步动作。
- 交付包生成后，可以在排期卡片里直接「打开交付包」。
- 候选稿卡片可直接复制执行说明和抖音发布文案，方便发微信。
- 排期卡片可以标记异常，避免卡在无法推进的内容上。
- 素材扫描后，可以把素材标记为「可用 / 需处理 / 不可用」。
- 素材缺口旁可以直接创建对应 Image 任务。
- 核对队列里点「核对并回填」可录入粉丝、播放、点赞、评论。
- 「爆款链接」页只要求工作人员粘贴抖音作品链接；系统先记录为待分析，后续由 Codex/AI 用青豆提取、拆结构，再按适合/禁用人设关键词批量生成。
- Image 任务可以打开输出目录，并标记 `review / approved / rejected`。
- 剪辑任务会生成 `剪辑任务单.txt`，可复制给剪辑人员，也可打开本地任务目录。
- 交付包会生成 `05-素材顺序清单.txt`，按顺序说明微信要发送的图片或视频。
- 视频/口播交付包会额外生成 `03-口播字幕文案.txt`、`04-剪辑要求.txt` 和 `07-成片回收说明.txt`。
- Image 任务会在输出目录生成 `图片任务说明.txt`，包含 prompt、negative prompt、用途和保存目录。
